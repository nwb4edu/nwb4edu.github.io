# Core dependencies for running nwb4edu notebooks (Lessons 1-4)
# These are the essential packages - subdependencies will be installed automatically

# NWB and data access (includes: pynwb, h5py, hdmf)
dandi>=0.45.1

# Allen Institute SDK (includes: numpy, pandas, scipy, matplotlib)
allensdk>=2.11.2

# Widgets for interactive exploration (includes: ipywidgets, pynwb)
nwbwidgets>=0.8.0

# Visualization
seaborn>=0.11.0

# Streaming data
remfile

# Additional dependencies for Lesson 5 (Greatest Hits)
pynapple
plotly
fsspec

# Jupyter environment
jupyter>=1.0.0
notebook>=6.0.0
